
export const enum SDP_LINE_HEAD {
  Mid = 'mid',
  Head = 'extmap',
}

export const enum TRACK_DIRECTION {
  SendOnly = 'sendonly',
  RecvOnly = 'recvonly',
  SendRecv = 'sendrecv',
  Unknown = 'unknown',
}